{
  "name": "antigravity2-zh-hant-tw",
  "version": "1.2.0",
  "private": true,
  "description": "Antigravity 2.0 繁體中文套件",
  "scripts": {
    "check": "node --check localization_engine.js && node --check auto-localization-maintainer.js && node validate-localization.js && node --test test-auto-localization-maintainer.js",
    "maintainer:status": "node auto-localization-maintainer.js --status",
    "version": "node localization_engine.js --version",
    "install:localization": "node localization_engine.js",
    "restore": "node localization_engine.js --restore",
    "release:package": "node scripts/create-release-package.js"
  },
  "dependencies": {
    "@electron/asar": "^3.3.1"
  }
}
